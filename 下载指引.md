# Signal 翻译中枢下载指引

这是独立的 Signal 翻译中枢下载项目，和 WhatsApp、通讯翻译中枢分开。

当前稳定版本：1.1.20

## 推荐下载：小引导安装包

点击下载：[yitaifang-signal-translator-bootstrap-v1.1.20.zip](https://github.com/canglang-88/yitaifang-signal-translator-release/releases/download/v1.1.20/yitaifang-signal-translator-bootstrap-v1.1.20.zip)

优先把这个小引导包发给用户。它会自动读取云端最新版本，显示下载进度，下载完整包后校验 SHA256，再覆盖安装。

## 备用下载：完整包

如果引导包被安全软件拦截，或者当前网络无法自动下载，可以手动下载完整包：

[yitaifang-signal-translator-mother-v1.1.20.zip](https://github.com/canglang-88/yitaifang-signal-translator-release/releases/download/v1.1.20/yitaifang-signal-translator-mother-v1.1.20.zip)

完整包内置 Signal 官方内核：8.15.0。

## 测试补丁：启动授权自检

这个包用于测试“中枢和子程序启动前读取同一份授权资产”的新逻辑：

[yitaifang-signal-startup-self-contained-v1.1.21-test.zip](https://github.com/canglang-88/yitaifang-signal-translator-release/releases/download/v1.1.20/yitaifang-signal-startup-self-contained-v1.1.21-test.zip)

文件大小：131964968 字节

SHA256：

```text
673227A72F80891686C88CB6282B6EA2BF4E1813D168D5388B119DE905D69B31
```

测试补丁只包含：

- `SignalSilentLauncher.exe`
- `SignalCopyGate.exe`
- `测试说明.txt`

它不包含、也不会修改：

- `YitaifangSignalTranslator.exe`
- `YitaifangSignalTranslator.dll`
- OpenAI API Key
- Signal 登录数据
- 聊天记录
- 账号数据
- Signal 官方内核

测试时，把 `SignalSilentLauncher.exe` 放到主程序根目录，把 `SignalCopyGate.exe` 放到每个账号目录。主程序根目录保留同一份 `signal-license-status.json`，中枢和子程序启动门都会读取这一份授权资产。

## 新安装

1. 下载小引导包或完整包。
2. 解压到任意文件夹。
3. 优先运行小引导包里的 `一键安装或覆盖.cmd`。
4. 如果使用完整包，运行 `Signal翻译中枢.exe` 或 `一键安装或覆盖.cmd`。
5. 填写 OpenAI Key，选择模型，保存配置。
6. 点击“新建 Signal 翻译版”，创建独立 Signal 翻译副本和桌面快捷方式。

## 旧版覆盖更新

1. 下载小引导包。
2. 解压后运行 `一键安装或覆盖.cmd`。
3. 脚本会优先从桌面快捷方式寻找旧版位置，关闭相关程序后覆盖程序文件。
4. 覆盖时会保留 `Signal翻译账号`、登录数据、聊天记录、OpenAI 配置、本机配置和已有副本。

## v1.1.20 重点

- 中枢保留授权、云端禁用、云端到期和“重新检测”入口。
- 子程序保持稳定翻译入口：`YitaifangSignalTranslator.exe`。
- 子程序保持稳定翻译核心：`YitaifangSignalTranslator.dll`。
- 不再把大体积门禁外壳覆盖到翻译入口，避免更新后翻译功能丢失。
- 更新中枢后，如果已有副本需要同步，请点击“更新全部副本”。
