# Signal 翻译中枢下载指引

这是独立的 Signal 翻译中枢下载项目，和 WhatsApp、通讯翻译中枢分开。

当前最新版本：1.2.4

## 推荐下载：小引导安装包

点击下载：[yitaifang-signal-translator-bootstrap-v1.2.4.zip](https://github.com/canglang-88/yitaifang-signal-translator-release/releases/download/v1.2.4/yitaifang-signal-translator-bootstrap-v1.2.4.zip)

请优先把这个小引导包发给用户。它只有几 KB，会自动：

1. 读取云端最新版。
2. 显示下载进度。
3. 下载完整包后校验 SHA256。
4. 校验通过后解压完整包。
5. 运行完整包里的 `一键安装或覆盖.cmd`。
6. 覆盖程序文件，但保留账号、聊天记录、OpenAI 配置和本机设置。

## 备用下载：完整包

如果引导包被安全软件拦截，或当前网络无法自动下载，可以手动下载完整包：

[yitaifang-signal-translator-mother-v1.2.4.zip](https://github.com/canglang-88/yitaifang-signal-translator-release/releases/download/v1.2.4/yitaifang-signal-translator-mother-v1.2.4.zip)

完整包内置 Signal 官方内核：8.15.0。

## 新安装

1. 下载小引导包或完整包。
2. 解压到任意文件夹。
3. 优先运行小引导包里的 `一键安装或覆盖.cmd`。
4. 如果使用完整包，运行 `Signal翻译中枢.exe` 或 `一键安装或覆盖.cmd`。
5. 填写 OpenAI Key，选择模型，保存配置。
6. 点击“新建 Signal 翻译版”，创建独立 Signal 翻译副本和桌面快捷方式。

## 旧版覆盖更新

1. 下载小引导包。
2. 解压后运行 `一键安装或覆盖.cmd`。
3. 脚本会优先从桌面快捷方式寻找旧版位置，强制关闭相关程序后覆盖程序文件。
4. 覆盖时会保留 `Signal翻译账号`、登录数据、聊天记录、OpenAI 配置、本机配置和已有副本。

## 本次更新

- 中枢保留授权、云端禁用、云端到期和“重新检测”入口。
- 子程序保持稳定翻译入口：`YitaifangSignalTranslator.exe` 184KB。
- 子程序保持稳定翻译核心：`YitaifangSignalTranslator.dll` 269KB。
- 不再把 153MB 门禁壳覆盖到翻译入口，避免更新后翻译功能丢失。
- 更新中枢后，如果已有副本需要同步，请点击“更新全部副本”。

## 不包含内容

- OpenAI API Key
- Signal 登录数据
- 聊天记录
- 翻译缓存
- 日志和本机配置备份
